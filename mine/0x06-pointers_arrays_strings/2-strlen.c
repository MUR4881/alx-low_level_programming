/**
 * _strlen - get the length of a given string
 * @s: the given string
 * Return: the length of string s
 */
int _strlen(char *s)
{
	int x;

	x = 0;
	for (; *(s + x) != '\0'; x++)
	{
	}
	return (x);
}
